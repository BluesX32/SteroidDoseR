% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{create_connection_from_discovery_env}
\alias{create_connection_from_discovery_env}
\title{Create a SAFER Desktop / Discovery HPC connection from an env file}
\usage{
create_connection_from_discovery_env(env_file = "R.env")
}
\arguments{
\item{env_file}{Path to the env file. Accepts \code{R.env} (REACH-Templates
convention), \code{.env}, or \code{~/.env} (Discovery HPC home-dir convention).
Tries each candidate in order if \code{env_file} does not exist.}
}
\value{
An \code{omop_connector} with \code{$use_rjdbc = TRUE}.
}
\description{
Loads credentials from an env file using \code{dotenv::load_dot_env()} (if
the \code{dotenv} package is installed) or the built-in parser, then calls
\code{\link[=create_discovery_connection]{create_discovery_connection()}}. This matches the usage pattern from
\code{REACH-Templates/scripts/databricks_connect.R}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# REACH-Templates pattern:
source("scripts/databricks_connect.R")
con <- connect_databricks("R.env")

# SteroidDoseR equivalent:
con <- create_connection_from_discovery_env("R.env")
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
con <- create_connection_from_discovery_env("R.env")
episodes <- run_pipeline(con, method = "baseline")
disconnect_connector(con)
}
}
