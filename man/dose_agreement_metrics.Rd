% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval.R
\name{dose_agreement_metrics}
\alias{dose_agreement_metrics}
\title{Compute dose-accuracy metrics from two numeric dose vectors}
\usage{
dose_agreement_metrics(computed, actual)
}
\arguments{
\item{computed}{Numeric vector of algorithm-computed doses.}

\item{actual}{Numeric vector of reference (gold or other-method) doses,
same length and pairing as \code{computed}.}
}
\value{
A one-row tibble: \code{n} (non-missing pairs used), \code{MAE}, \code{MBE},
\code{RMSE}, \code{median_AE}, \code{MAPE}, \code{mean_relative_bias_pct}, \code{pearson_corr},
\code{spearman_corr}. Correlations are \code{NA} when fewer than 3 pairs are
available (matching the guard in \code{\link[=evaluate_against_gold]{evaluate_against_gold()}}).
}
\description{
Standalone version of the metric block inside \code{\link[=evaluate_against_gold]{evaluate_against_gold()}}
(MAE, MBE, RMSE, median AE, MAPE, mean relative bias, Pearson/Spearman
correlation), taking plain numeric vectors instead of episode data frames.
\code{\link[=evaluate_against_gold]{evaluate_against_gold()}} itself does not call this -- it keeps its
original inlined computation unchanged. This helper exists so that other
comparisons computed at a different granularity (e.g. per-visit dose from
\code{\link[=dose_at_visits]{dose_at_visits()}}) can be scored with the exact same formulas as the
episode-level numbers, without duplicating them by hand.
}
\examples{
dose_agreement_metrics(c(10, 20, 5), c(10, 15, 5))
}
\seealso{
\code{\link[=evaluate_against_gold]{evaluate_against_gold()}}, \code{\link[=dose_at_visits]{dose_at_visits()}}
}
