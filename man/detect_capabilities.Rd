% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connector.R
\name{detect_capabilities}
\alias{detect_capabilities}
\title{Detect which drug_exposure fields are available in the data source}
\usage{
detect_capabilities(connector)
}
\arguments{
\item{connector}{A \code{steroid_connector} object.}
}
\value{
A modified \code{connector} with \verb{$capabilities} populated:
\describe{
\item{\code{has_sig}}{\code{TRUE} if \code{drug_exposure.sig} is available.}
\item{\code{has_days_supply}}{\code{TRUE} if \code{days_supply} is available.}
\item{\code{has_quantity}}{\code{TRUE} if \code{quantity} is available.}
\item{\code{has_route}}{\code{TRUE} if \code{route_concept_id} is available.}
\item{\code{has_drug_source}}{\code{TRUE} if \code{drug_source_value} is available.}
\item{\code{has_drug_concept}}{\code{TRUE} if \code{drug_concept_id} is available.}
}
}
\description{
Probes the data source and returns a named logical list indicating whether
each optional OMOP \code{drug_exposure} field is present. The result is stored
in \code{connector$capabilities} and used internally to select imputation
methods and fallback strategies.
}
\details{
For \code{omop_connector}: executes lightweight \verb{WHERE 1=0} probe queries
against the live database.

For \code{df_connector}: inspects the column names of the wrapped data frame.
}
