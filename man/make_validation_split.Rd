% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval.R
\name{make_validation_split}
\alias{make_validation_split}
\title{Split a gold-standard data frame into train and validate folds by patient}
\usage{
make_validation_split(gold_df, id_col = "person_id", train_frac = 0.7, seed = 42L)
}
\arguments{
\item{gold_df}{Data frame of gold-standard episodes. Must contain \code{id_col}.}

\item{id_col}{\code{character(1)}. Patient ID column. Default: \code{"person_id"}.}

\item{train_frac}{\code{numeric(1)}. Fraction of patients to place in the
training fold. Default: \code{0.7} (70 \% train / 30 \% validate).}

\item{seed}{\code{integer(1)}. Random seed for reproducibility. Default: \code{42L}.}
}
\value{
A named list:
\describe{
\item{\code{$train}}{Gold episodes for training patients (use for tuning).}
\item{\code{$validate}}{Gold episodes for validation patients (use for evaluation).}
\item{\code{$train_ids}}{Integer vector of patient IDs in the training fold.}
\item{\code{$validate_ids}}{Integer vector of patient IDs in the validation fold.}
}
}
\description{
Randomly assigns each unique patient to either the training set or the
validation set, returning two non-overlapping subsets of \code{gold_df}. Always
splits by patient (not by episode) so the same patient never appears in
both folds.

\strong{Why this matters for threshold tuning:} If you tune hierarchical
thresholds (\code{match_tol}, \code{diff_threshold}) using
\code{\link[=tune_hierarchical_thresholds]{tune_hierarchical_thresholds()}} and then evaluate those same
thresholds on the \emph{full} gold set, the performance estimate is optimistic
(gold-standard leakage). Use the \verb{$train} split for tuning and the
\verb{$validate} split for unbiased evaluation.
}
\examples{
gold <- tibble::tibble(
  person_id         = c(1L, 1L, 2L, 3L, 4L),
  episode_start     = as.Date(c("2023-01-01","2023-06-01",
                                "2023-01-01","2023-03-01","2023-02-01")),
  episode_end       = as.Date(c("2023-05-31","2023-12-31",
                                "2023-06-30","2023-09-30","2023-08-31")),
  median_daily_dose = c(20, 10, 40, 5, 20)
)
split <- make_validation_split(gold, train_frac = 0.75, seed = 1L)
length(split$train_ids)
nrow(split$validate)
}
\seealso{
\code{\link[=tune_hierarchical_thresholds]{tune_hierarchical_thresholds()}} — pass \code{gold_df = split$train}.
\code{\link[=stratify_errors_by_parse]{stratify_errors_by_parse()}} — evaluate on \code{split$validate}.
}
