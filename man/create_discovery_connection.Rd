% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{create_discovery_connection}
\alias{create_discovery_connection}
\title{Create a Databricks connection using the REACH-Templates env var convention}
\usage{
create_discovery_connection(
  host = NULL,
  http_path = NULL,
  token = NULL,
  jdbc_jar = NULL,
  data_catalog = NULL,
  user_catalog = NULL,
  username = NULL,
  cdm_schema = NULL,
  vocab_schema = NULL,
  results_schema = NULL,
  use_env = TRUE
)
}
\arguments{
\item{host}{Databricks workspace URL (with \code{https://}) or hostname.}

\item{http_path}{SQL Warehouse HTTP path.}

\item{token}{Personal Access Token.}

\item{jdbc_jar}{Path to the Databricks JDBC driver jar. Auto-detected from
\code{~/jdbc/} when \code{NULL}.}

\item{data_catalog}{Top-level data catalog. Default \code{"deid"}.}

\item{user_catalog}{User scratch catalog. Default \code{"reach_users"}.}

\item{username}{Your JHED ID (used to build the results schema).}

\item{cdm_schema}{Full CDM schema (\code{catalog.schema}). When \code{NULL}, auto-built
as \code{paste0(data_catalog, ".omop")}.}

\item{vocab_schema}{Vocabulary schema. Defaults to \code{cdm_schema}.}

\item{results_schema}{Results schema. When \code{NULL}, auto-built as
\code{paste0(user_catalog, ".", username)} (if \code{username} is known).}

\item{use_env}{Logical. Load unset parameters from environment variables.
Default \code{TRUE}.}
}
\value{
An \code{omop_connector} with \code{$use_rjdbc = TRUE}. Pass directly to
\code{\link[=run_pipeline]{run_pipeline()}}, \code{\link[=fetch_drug_exposure]{fetch_drug_exposure()}}, etc.
Call \code{\link[=disconnect_connector]{disconnect_connector()}} when finished.
}
\description{
Connects to Databricks using \code{rJava} + \code{RJDBC} + \code{DBI}, reading credentials
from the REACH-Templates standard env vars (\code{DATABRICKS_HOST},
\code{DATABRICKS_DATA_CATALOG}, \code{DATABRICKS_USER_CATALOG},
\code{DATABRICKS_USERNAME}). This mirrors the \code{connect_databricks()} helper in
\code{REACH-Templates/scripts/databricks_connect.R} and works from both SAFER
Desktop (Windows Citrix) and Discovery HPC (Linux OnDemand).

Auto-constructs \code{cdm_schema} as \code{{data_catalog}.omop} (e.g. \code{"deid.omop"})
and \code{results_schema} as \code{{user_catalog}.{username}} (e.g.
\code{"reach_users.mxiong5"}) when not set explicitly.
}
\examples{
\dontrun{
# Simplest: load from the REACH-Templates R.env file
con <- create_connection_from_discovery_env("~/REACH-Templates/R.env")
drug_df <- with_connector(con, function(active) {
  fetch_drug_exposure(active, start_date = "2020-01-01")
})
episodes <- run_pipeline(drug_df, method = "baseline")
disconnect_connector(con)

# Explicit arguments
con <- create_discovery_connection(
  host        = "https://adb-1234.7.azuredatabricks.net",
  http_path   = "/sql/1.0/warehouses/abc123",
  token       = Sys.getenv("DATABRICKS_TOKEN"),
  username    = "mxiong5"
)
}
}
