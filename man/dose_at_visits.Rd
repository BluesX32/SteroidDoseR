% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/episodes.R
\name{dose_at_visits}
\alias{dose_at_visits}
\title{Look up the active dose at a set of point-in-time visit dates}
\usage{
dose_at_visits(
  episodes_df,
  visits_df,
  person_col = "person_id",
  visit_date_col = "visit_date",
  episode_start_col = "episode_start",
  episode_end_col = "episode_end",
  dose_col = "mean_daily_dose",
  no_coverage_value = 0
)
}
\arguments{
\item{episodes_df}{A data frame of episodes -- typically the output of
\code{\link[=build_episodes]{build_episodes()}}, or a gold-standard data frame from
\code{\link[=parse_steroid_gold]{parse_steroid_gold()}}. Must contain \code{person_col}, \code{episode_start_col},
\code{episode_end_col}, and \code{dose_col}.}

\item{visits_df}{A data frame of point-in-time encounters (e.g. office
visits from \code{\link[=fetch_visit_occurrence]{fetch_visit_occurrence()}}). Must contain \code{person_col} and
\code{visit_date_col}.}

\item{person_col}{\code{character(1)}. Patient identifier column, shared by
both data frames. Default \code{"person_id"}.}

\item{visit_date_col}{\code{character(1)}. Encounter date column in
\code{visits_df}. Default \code{"visit_date"}.}

\item{episode_start_col, episode_end_col}{\code{character(1)}. Episode window
columns in \code{episodes_df}. Defaults \code{"episode_start"}, \code{"episode_end"}.}

\item{dose_col}{\code{character(1)}. Dose column in \code{episodes_df}. Default
\code{"mean_daily_dose"}.}

\item{no_coverage_value}{\code{numeric(1)}. Value assigned to \code{dose_mg} when no
episode covers the visit date. Default \code{0} -- no active episode means
"not on steroids" per that method, a real and comparable clinical state.
Pass \code{NA_real_} when \code{episodes_df} is a gold standard: a coverage gap
there means "not chart-reviewed at this date," not "confirmed off
steroids," and should not be scored as a 0 mg agreement.}
}
\value{
\code{visits_df} with two columns appended: \code{dose_mg} (numeric; summed
across covering episodes, \code{NA} if all covering episodes have unknown
dose, \code{no_coverage_value} if none cover the date) and \code{has_coverage}
(logical, \code{TRUE} when at least one episode covered the visit date).
}
\description{
Cross-sectional companion to \code{\link[=evaluate_against_gold]{evaluate_against_gold()}}'s episode-level
comparison. For each row in \code{visits_df}, finds every row in \code{episodes_df}
for the same patient whose \verb{[episode_start_col, episode_end_col]} window
contains \code{visit_date_col}, and sums \code{dose_col} across all covering episodes
-- this matters when a patient is mid cross-titration between two different
steroid drugs, since both episodes contribute to total steroid burden on
that date. Visits with no covering episode get \code{no_coverage_value}.
}
\examples{
episodes <- tibble::tibble(
  person_id       = 1L,
  episode_start   = as.Date("2023-01-01"),
  episode_end     = as.Date("2023-03-31"),
  mean_daily_dose = 10
)
visits <- tibble::tibble(
  person_id  = c(1L, 1L),
  visit_date = as.Date(c("2023-02-01", "2023-06-01"))
)
dose_at_visits(episodes, visits)
}
\seealso{
\code{\link[=build_episodes]{build_episodes()}}, \code{\link[=evaluate_against_gold]{evaluate_against_gold()}},
\code{\link[=dose_agreement_metrics]{dose_agreement_metrics()}}, \code{\link[=fetch_visit_occurrence]{fetch_visit_occurrence()}}
}
