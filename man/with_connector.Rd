% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connector.R
\name{with_connector}
\alias{with_connector}
\title{Execute a function within a managed database connection}
\usage{
with_connector(connector, fn, ...)
}
\arguments{
\item{connector}{A \code{steroid_connector} object.}

\item{fn}{A function that accepts a single argument (the connector, with
\verb{$conn} set for omop connectors).}

\item{...}{Additional arguments forwarded to \code{fn}.}
}
\value{
The return value of \code{fn(connector, ...)}.
}
\description{
For \code{omop_connector}: opens a connection, runs \code{fn(connector)}, then
closes the connection — even if \code{fn} throws an error.
}
\details{
For \code{df_connector}: runs \code{fn(connector)} directly (no connection needed).
}
