% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gold_standard.R
\name{compare_dmard_episodes}
\alias{compare_dmard_episodes}
\title{Compare DMARD gold-standard records against computed drug episodes}
\usage{
compare_dmard_episodes(
  computed_df,
  gold_df,
  computed_id_col = "person_id",
  computed_drug_col = "drug_name_std",
  computed_start_col = "episode_start",
  computed_end_col = "episode_end",
  computed_dose_col = "median_daily_dose",
  gold_id_col = "person_id",
  gold_drug_col = "drug_name_std",
  gold_start_col = "episode_start",
  gold_end_col = "episode_end",
  gold_dose_col = "dose_daily_mg_equiv",
  min_overlap_days = 1L
)
}
\arguments{
\item{computed_df}{Data frame of computed drug episodes (output of
\code{\link[=build_episodes]{build_episodes()}}). Must contain columns named by \code{computed_id_col},
\code{computed_drug_col}, \code{computed_start_col}, \code{computed_end_col}, and
\code{computed_dose_col}.}

\item{gold_df}{Data frame of parsed gold-standard records (output of
\code{\link[=parse_dmard_gold]{parse_dmard_gold()}}). Must contain columns named by \code{gold_id_col},
\code{gold_drug_col}, \code{gold_start_col}, \code{gold_end_col}, and \code{gold_dose_col}.}

\item{computed_id_col}{\code{character(1)}. Patient ID in \code{computed_df}.
Default: \code{"person_id"}.}

\item{computed_drug_col}{\code{character(1)}. Drug name in \code{computed_df}.
Default: \code{"drug_name_std"}.}

\item{computed_start_col}{\code{character(1)}. Episode start in \code{computed_df}.
Default: \code{"episode_start"}.}

\item{computed_end_col}{\code{character(1)}. Episode end in \code{computed_df}.
Default: \code{"episode_end"}.}

\item{computed_dose_col}{\code{character(1)}. Dose column in \code{computed_df}.
Default: \code{"median_daily_dose"}.}

\item{gold_id_col}{\code{character(1)}. Patient ID in \code{gold_df}.
Default: \code{"person_id"}.}

\item{gold_drug_col}{\code{character(1)}. Drug name in \code{gold_df}.
Default: \code{"drug_name_std"}.}

\item{gold_start_col}{\code{character(1)}. Gold window start in \code{gold_df}.
Default: \code{"episode_start"}.}

\item{gold_end_col}{\code{character(1)}. Gold window end in \code{gold_df}.
Default: \code{"episode_end"}.}

\item{gold_dose_col}{\code{character(1)}. Gold dose in \code{gold_df}.
Default: \code{"dose_daily_mg_equiv"}.}

\item{min_overlap_days}{\code{integer(1)}. Minimum overlap in days for a
computed episode to count. Default: \code{1L}.}
}
\value{
A named list with three elements:
\describe{
\item{\code{$comparison}}{One row per gold record. Key columns:
\code{person_id}, \code{drug_name_std}, \code{gold_start}, \code{gold_end},
\code{gold_dose}, \code{computed_dose} (duration-weighted mean),
\code{n_computed_episodes}, \code{total_overlap_days}, \code{gold_duration},
\code{overlap_pct}, \code{absolute_error}, \code{bias_error},
\code{relative_error_pct}, \code{absolute_relative_error_pct},
\code{agreement_category}, \code{error_direction}.}
\item{\code{$summary}}{One-row tibble: \code{n_gold_records},
\code{n_matched_records}, \code{coverage_pct}, \code{MAE}, \code{MBE}, \code{RMSE},
\code{median_AE}, \code{MAPE}, \code{mean_relative_bias_pct},
\code{pearson_corr}, \code{spearman_corr}.}
\item{\code{$stratified}}{List with \code{by_drug} (metrics per
\code{drug_name_std}).}
}
}
\description{
For each gold-standard record (patient + drug + date window), finds all
computed episodes for the same patient that overlap the gold window (joined
by patient ID only, mirroring \code{\link[=evaluate_against_gold]{evaluate_against_gold()}}). Clips each
overlapping computed episode to the intersection, then computes a
duration-weighted mean dose across all overlapping episodes. Error metrics
are calculated against the gold dose.

All column name arguments have defaults matching the output of
\code{\link[=parse_dmard_gold]{parse_dmard_gold()}} and \code{\link[=build_episodes]{build_episodes()}}, but every name can be
overridden.
}
\examples{
computed <- tibble::tibble(
  person_id         = 1L,
  drug_name_std     = "methotrexate",
  episode_start     = as.Date("2020-01-01"),
  episode_end       = as.Date("2022-12-31"),
  median_daily_dose = 15 / 7
)
gold <- tibble::tibble(
  person_id           = 1L,
  drug_name_std       = "methotrexate",
  episode_start       = as.Date("2020-06-01"),
  episode_end         = as.Date("2022-06-01"),
  dose_daily_mg_equiv = 15 / 7
)
compare_dmard_episodes(computed, gold)
}
