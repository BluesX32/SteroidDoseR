% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conversion.R
\name{convert_pred_equiv}
\alias{convert_pred_equiv}
\title{Convert raw daily doses to prednisone-equivalent mg/day}
\usage{
convert_pred_equiv(
  drug_df,
  drug_col = "drug_name_std",
  dose_col = "daily_dose_mg",
  out_col = "pred_equiv_mg",
  equiv_table = NULL,
  drug_name_map = NULL
)
}
\arguments{
\item{drug_df}{A data frame containing at least a drug-name column and a
numeric daily-dose column.}

\item{drug_col}{\code{character(1)}. Name of the column holding drug names.
Values are standardised internally (lowercase, trimmed, synonym-mapped).
Default: \code{"drug_name_std"}. The function also accepts \code{"dmard_name"} or
\code{"drug_concept_name"} -- whichever is present if \code{drug_col} is not found,
a fallback search is attempted.}

\item{dose_col}{\code{character(1)}. Name of the numeric daily-dose column.
Default: \code{"daily_dose_mg"}.}

\item{out_col}{\code{character(1)}. Name of the output column for
prednisone-equivalent dose. Default: \code{"pred_equiv_mg"}.}

\item{equiv_table}{A data frame with columns \code{drug_name_std} and
\code{equiv_factor}. If \code{NULL} (default), the built-in clinical table is used.}

\item{drug_name_map}{Optional data frame with columns \code{pattern} (regex) and
\code{canonical_name} for custom drug-name mappings passed to
\code{\link[=standardize_drug_name]{standardize_drug_name()}}. \code{NULL} uses built-in mappings.}
}
\value{
\code{drug_df} with three additional columns:
\itemize{
\item \strong{\verb{<out_col>}} (\code{numeric}): prednisone-equivalent daily dose (mg/day).
\code{NA} when the equivalency factor is unknown or missing.
\item \strong{\code{equiv_factor}} (\code{numeric}): the multiplicative factor applied.
\item \strong{\code{pred_equiv_status}} (\code{character}): one of \code{"ok"},
\code{"missing_factor"} (drug known but factor is \code{NA}, e.g. budesonide), or
\code{"unknown_drug"} (drug not in the equivalency table).
}
}
\description{
Joins a built-in equivalency table (or a user-supplied one) to multiply
each drug's raw daily dose by its potency factor relative to prednisone.
}
\examples{
df <- tibble::tibble(
  drug_name_std = c("prednisone", "methylprednisolone", "dexamethasone"),
  daily_dose_mg = c(10, 8, 4)
)
convert_pred_equiv(df)
}
