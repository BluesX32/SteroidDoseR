% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/episodes.R
\name{gap_sensitivity}
\alias{gap_sensitivity}
\title{Assess sensitivity of episode structure to gap_days assumption}
\usage{
gap_sensitivity(drug_df, gap_grid = c(0L, 7L, 14L, 30L, 60L, 90L), ...)
}
\arguments{
\item{drug_df}{Data frame of dose-imputed drug-exposure records (the same
object you would pass to \code{\link{build_episodes}()}).}

\item{gap_grid}{\code{integer} vector. Gap values to test. Default:
\code{c(0L, 7L, 14L, 30L, 60L, 90L)}.}

\item{...}{Additional arguments forwarded to \code{\link{build_episodes}()}
(e.g. \code{dose_col}, \code{end_col}, \code{person_col}, \code{drug_col}).}
}
\value{
A tibble with one row per \code{gap_days} value and columns:
\describe{
  \item{gap_days}{The gap parameter tested.}
  \item{n_episodes}{Total number of episodes produced.}
  \item{median_episode_days}{Median episode duration (days).}
  \item{p25_episode_days}{25th percentile episode duration.}
  \item{p75_episode_days}{75th percentile episode duration.}
  \item{n_patients}{Number of distinct \code{person_id} values.}
  \item{episodes_per_patient}{Mean episodes per patient.}
}
}
\description{
Calls \code{\link{build_episodes}()} repeatedly across a grid of \code{gap_days} values and
returns summary statistics so you can choose a clinically defensible
bridging window. Typical OHDSI analyses use 30 days; real-world scripts
use 7--90 days.
}
\examples{
\dontrun{
df <- calc_daily_dose_nlp_advanced(drug_df)
gap_sensitivity(df, gap_grid = c(0L, 7L, 30L, 90L))
}
}
