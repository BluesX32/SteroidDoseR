% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{create_safer_connection}
\alias{create_safer_connection}
\title{Create a Databricks connection for the SAFER / REACH HPC environment}
\usage{
create_safer_connection(
  server_hostname = NULL,
  http_path = NULL,
  token = NULL,
  jdbc_jar = NULL,
  cdm_schema = NULL,
  vocab_schema = NULL,
  results_schema = NULL,
  proxy_host = NULL,
  proxy_port = NULL,
  use_env = TRUE
)
}
\arguments{
\item{server_hostname}{Databricks workspace hostname.
Strip the leading \code{https://} if present.}

\item{http_path}{SQL Warehouse or cluster HTTP path.}

\item{token}{Personal Access Token (\code{dapi...}).}

\item{jdbc_jar}{Path to the Databricks JDBC driver \code{.jar}. Auto-detected
from common locations when \code{NULL}: \code{C:/jdbc/databricks-jdbc-2.6.36.jar}
(SAFER Desktop), \code{~/jdbc/databricks-jdbc-2.6.36.jar} (Discovery HPC), or
the package's bundled \code{jdbc_drivers/databricks/} folder.}

\item{cdm_schema}{CDM schema in \code{catalog.schema} format, e.g. \code{"deid.omop"}.}

\item{vocab_schema}{Vocabulary schema (defaults to \code{cdm_schema}).}

\item{results_schema}{Results / scratch schema (optional).}

\item{proxy_host}{HTTP proxy hostname. Set \code{DATABRICKS_PROXY_HOST} in your
env file (e.g. \code{proxy.jh.edu}) or supply directly. Required on SAFER
Desktop; leave \code{NULL} on Discovery HPC.}

\item{proxy_port}{HTTP proxy port number. Set \code{DATABRICKS_PROXY_PORT} in
your env file (e.g. \code{3129}) or supply directly.}

\item{use_env}{Logical. Load unset parameters from environment variables.
Default \code{TRUE}.}
}
\value{
An \code{omop_connector} object (\code{c("omop_connector", "steroid_connector")})
with an open RJDBC/DBI connection and \code{$use_rjdbc = TRUE}.
}
\description{
Uses \code{rJava}, \code{RJDBC}, and \code{DBI} instead of \code{DatabaseConnector} to connect
to a Databricks SQL Warehouse. This is the native approach for the Johns
Hopkins SAFER / REACH HPC cluster where the Databricks JDBC driver is
accessed directly without the OHDSI DatabaseConnector layer.

The returned connector satisfies the same interface as the one produced by
\code{\link[=create_omop_connection]{create_omop_connection()}}: pass it directly to \code{\link[=run_pipeline]{run_pipeline()}},
\code{\link[=fetch_drug_exposure]{fetch_drug_exposure()}}, \code{\link[=detect_capabilities]{detect_capabilities()}}, etc.
Call \code{\link[=disconnect_connector]{disconnect_connector()}} when finished.

\strong{SAFER Desktop proxy:} Direct HTTPS connections to Azure Databricks are
blocked on SAFER Desktop. Set \code{DATABRICKS_PROXY_HOST=proxy.jh.edu} and
\code{DATABRICKS_PROXY_PORT=3129} in your \code{R.env} file, or pass \code{proxy_host}
and \code{proxy_port} directly. This is not required on Discovery HPC.

\strong{JDBC driver:} Place the driver at \code{C:/jdbc/databricks-jdbc-2.6.36.jar}
(SAFER Desktop) or \code{~/jdbc/databricks-jdbc-2.6.36.jar} (Discovery HPC).
Download from Maven Central:
\url{https://repo1.maven.org/maven2/com/databricks/databricks-jdbc/2.6.36/}
}
\examples{
\dontrun{
# Simplest: load everything from R.env
# R.env must include DATABRICKS_PROXY_HOST / DATABRICKS_PROXY_PORT on SAFER Desktop
con <- create_connection_from_safer_env("R.env")
drug_df <- with_connector(con, function(active) {
  fetch_drug_exposure(active, start_date = "2020-01-01")
})
episodes <- run_pipeline(drug_df, method = "baseline")
disconnect_connector(con)

# Explicit arguments (SAFER Desktop with proxy)
con <- create_safer_connection(
  server_hostname = "adb-1234.7.azuredatabricks.net",
  http_path       = "/sql/1.0/warehouses/abc123",
  token           = Sys.getenv("DATABRICKS_TOKEN"),
  cdm_schema      = "deid.omop",
  proxy_host      = "proxy.jh.edu",
  proxy_port      = 3129L
)
}
}
