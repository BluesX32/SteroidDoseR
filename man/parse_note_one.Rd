% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlp_notes.R
\name{parse_note_one}
\alias{parse_note_one}
\title{Parse one clinical note string with medspaCy}
\usage{
parse_note_one(note_text)
}
\arguments{
\item{note_text}{\code{character(1)}. Raw clinical note text.}
}
\value{
A one-row \code{tibble} with the same columns as \code{\link[=parse_sig_one]{parse_sig_one()}} plus:
\describe{
\item{note_entity_count}{Number of steroid entities found.}
\item{note_negation_flag}{\code{TRUE} if the steroid mention is negated.}
\item{note_uncertainty_flag}{\code{TRUE} if the mention is uncertain/hedged.}
\item{note_section}{Detected note section (\code{"history"}, \code{"assessment"},
\code{"plan"}, etc.), or \code{NA}.}
}

\code{parsed_status} is one of:
\describe{
\item{\code{"notes_ok"}}{Entity found with extractable dose.}
\item{\code{"notes_negated"}}{Drug negated — patient is NOT on it.}
\item{\code{"notes_uncertain"}}{Hedged mention (may/possibly).}
\item{\code{"notes_historical"}}{Drug mentioned only in past context.}
\item{\code{"notes_no_dose"}}{Steroid found but no dose amount.}
\item{\code{"notes_no_drug"}}{No steroid entity found.}
\item{\code{"empty"}}{Input was blank or \code{NA}.}
}
}
\description{
Extracts steroid dose information from unstructured clinical note text using
a medspaCy NLP pipeline. The function \strong{never throws an error} -- malformed
or empty inputs return a row of \code{NA}s with an appropriate \code{parsed_status}.

Requires Python, medspaCy, and (optionally) the scispaCy biomedical model:

\if{html}{\out{<div class="sourceCode">}}\preformatted{pip install medspacy scispacy
python -m spacy download en_core_sci_sm
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
parse_note_one("Patient continued on prednisone 40 mg daily.")
parse_note_one("Patient is NOT on any steroids.")
parse_note_one("May consider starting methylprednisolone 1 g IV.")
parse_note_one(NA_character_)
}
}
\seealso{
\code{\link[=parse_notes]{parse_notes()}}, \code{\link[=calc_daily_dose_nlp_notes]{calc_daily_dose_nlp_notes()}}
}
