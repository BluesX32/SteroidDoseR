% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlp_notes.R
\name{parse_notes}
\alias{parse_notes}
\title{Apply \code{parse_note_one()} to every row of a data frame}
\usage{
parse_notes(drug_df, note_col = "clinical_note")
}
\arguments{
\item{drug_df}{A data frame containing a clinical note column.}

\item{note_col}{\code{character(1)}. Name of the note text column.
Default: \code{"clinical_note"}.}
}
\value{
\code{drug_df} with parsed columns from \code{\link[=parse_note_one]{parse_note_one()}} appended
(excluding the \code{sig_raw} duplicate).
}
\description{
Apply \code{parse_note_one()} to every row of a data frame
}
\examples{
\dontrun{
df <- tibble::tibble(
  person_id     = 1:2,
  clinical_note = c(
    "Patient on prednisone 40 mg daily.",
    "No steroids currently prescribed."
  )
)
parse_notes(df, note_col = "clinical_note")
}
}
\seealso{
\code{\link[=parse_note_one]{parse_note_one()}}, \code{\link[=calc_daily_dose_nlp_notes]{calc_daily_dose_nlp_notes()}}
}
