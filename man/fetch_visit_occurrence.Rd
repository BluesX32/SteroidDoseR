% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connector.R
\name{fetch_visit_occurrence}
\alias{fetch_visit_occurrence}
\title{Fetch office-visit encounters for cross-sectional dose comparison}
\usage{
fetch_visit_occurrence(
  connector_or_df,
  visit_concept_ids = NULL,
  person_ids = NULL,
  start_date = NULL,
  end_date = NULL
)
}
\arguments{
\item{connector_or_df}{An \code{omop_connector} (from \code{\link[=create_omop_connector]{create_omop_connector()}})
\strong{or} a data frame already containing \code{person_id}, \code{visit_start_date},
and \code{visit_concept_id} columns.}

\item{visit_concept_ids}{Integer vector of \code{visit_concept_id} values that
define "office visit" for this study, or \code{NULL} to include all visit
types.}

\item{person_ids}{Integer vector of \code{person_id} values to restrict to, or
\code{NULL} for all patients.}

\item{start_date, end_date}{Character/Date bounds on \code{visit_start_date}.
Default \code{NULL} (no bound on that side).}
}
\value{
A tibble with \code{person_id}, \code{visit_occurrence_id},
\code{visit_start_date} (\code{Date}), and \code{visit_concept_id}.
}
\description{
Retrieves \code{visit_occurrence} records -- the encounter dates used by
\code{\link[=dose_at_visits]{dose_at_visits()}} to ask "what dose was active on this specific visit
date" rather than over an entire gap-bridged episode. Accepts either a
live \code{omop_connector} (runs \code{inst/sql/extract_visit_occurrence.sql}) or a
plain data frame already containing visit rows (e.g. loaded from a
synthetic/CSV fixture) -- in the latter case the same filters are applied
directly to the data frame, no database required.
}
\details{
\code{visit_concept_ids} has no default. "Office visit" is a clinical/site
definition (e.g. OMOP standard concept \code{9202} "Outpatient Visit" is a
common starting point, but sites vary) that should be confirmed rather
than silently assumed.
}
\seealso{
\code{\link[=dose_at_visits]{dose_at_visits()}}, \code{\link[=fetch_drug_exposure]{fetch_drug_exposure()}}
}
