% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline.R
\name{calc_daily_dose_baseline}
\alias{calc_daily_dose_baseline}
\title{Compute daily steroid doses using structured OMOP fields (Baseline method)}
\usage{
calc_daily_dose_baseline(
  connector_or_df,
  methods = c("original", "tablets_freq", "actual_duration", "supply_based"),
  drug_concept_ids = NULL,
  person_ids = NULL,
  start_date = NULL,
  end_date = NULL,
  sig_source = "sig",
  m2_sig_parse = c("auto", "warn", "none"),
  max_daily_dose_mg = 2000,
  filter_oral = TRUE,
  equiv_table = NULL,
  drug_name_map = NULL
)
}
\arguments{
\item{connector_or_df}{A \code{steroid_connector} (from \code{\link[=create_omop_connector]{create_omop_connector()}}
or \code{\link[=create_df_connector]{create_df_connector()}}) \strong{or} a data frame. When a connector is
supplied the function fetches data from the source; when a data frame is
supplied the legacy in-memory path is used unchanged.}

\item{methods}{\code{character} vector. Ordered list of methods to attempt.
Defaults to \code{c("original", "tablets_freq", "actual_duration", "supply_based")}.
\code{"actual_duration"} (Burkard formula) is tried before \code{"supply_based"} because
the date-range denominator is the OHDSI standard for fixed-amount oral formulations.
Each method is only tried if the required columns are present; missing
columns cause a graceful skip (not an error).}

\item{drug_concept_ids}{Integer vector of OMOP \code{drug_concept_id} values to
restrict the extraction. Ignored when \code{connector_or_df} is a data frame.}

\item{person_ids}{Vector of \code{person_id} values to restrict the extraction.
Ignored when \code{connector_or_df} is a data frame.}

\item{start_date}{Character or Date. Lower bound on
\code{drug_exposure_start_date}. Ignored when \code{connector_or_df} is a data frame.}

\item{end_date}{Character or Date. Upper bound on
\code{drug_exposure_start_date}. Ignored when \code{connector_or_df} is a data frame.}

\item{sig_source}{\code{character(1)}. Which column to use as SIG text when the
native \code{sig} field is absent. One of \code{"sig"} (default) or
\code{"drug_source_value"}. Ignored when \code{connector_or_df} is a data frame.}

\item{m2_sig_parse}{\code{character(1)}. Controls behaviour when \code{tablets} and
\code{freq_per_day} columns are absent but a \code{sig} column is present:
\describe{
\item{\code{"auto"} (default)}{Call \code{\link[=parse_sig]{parse_sig()}} internally to populate
\code{tablets} and \code{freq_per_day} before running M2. This is the recommended
setting for real-world OMOP data where these columns are not pre-populated.}
\item{\code{"warn"}}{Emit a warning and skip M2.}
\item{\code{"none"}}{Silently skip M2 without any message.}
}
Ignored when \code{tablets} and \code{freq_per_day} columns already exist in the
data (even if all NA), or when \code{"tablets_freq"} is not in \code{methods}.}

\item{max_daily_dose_mg}{\code{numeric(1)} or \code{NULL}. Upper plausibility bound in
mg/day. Records whose imputed dose exceeds this value are set to \code{NA} with
a warning, because doses that large almost always indicate data-quality
problems (non-mg \code{amount_value}, quantity in total-mg, near-zero
\code{days_supply}, etc.). Default: \code{2000}. Set to \code{NULL} to disable the cap.}

\item{filter_oral}{\code{logical(1)}. If \code{TRUE} (default), restrict to oral-route
records and to drugs present in the prednisone-equivalency table before
imputing — matching the default behaviour of \code{\link[=calc_daily_dose_nlp]{calc_daily_dose_nlp()}}.
Set to \code{FALSE} only if the input is already pre-filtered to oral steroids.}

\item{equiv_table}{Optional data frame with the prednisone-equivalency table.
Must contain columns \code{drug_name_std} and \code{pred_equiv_factor}. When \code{NULL}
(default), the built-in \code{.pred_equiv_table} is used.}

\item{drug_name_map}{Optional data frame passed to \code{\link[=standardize_drug_name]{standardize_drug_name()}}
for site-specific drug name overrides. See that function for the required
\code{pattern}/\code{canonical_name} column format. Default: \code{NULL}.}
}
\value{
The input data frame (or the fetched data frame) with seven
additional columns:
\describe{
\item{strength_mg}{Extracted tablet/capsule strength in mg.}
\item{dose_from_original}{M1 value: the original \code{daily_dose} or
\code{daily_dose_mg} column cast to numeric (NA if absent or non-positive).}
\item{dose_from_tablets_freq}{M2 value: \code{tablets * freq_per_day * strength_mg} (NA if any component is missing).}
\item{dose_from_actual_duration}{M3 value: \code{(quantity * strength_mg) / actual_duration_days} (NA if any component is missing or duration <= 0).
This is the Burkard (2024) formula for fixed-amount oral formulations.}
\item{dose_from_supply}{M4 value: \code{(quantity * strength_mg) / days_supply}
(NA if any component is missing or \code{days_supply} is 0). Fallback only.}
\item{daily_dose_mg_imputed}{Best-estimate daily dose in mg: the first
non-NA value across M1-M4 in cascade order.}
\item{imputation_method}{One of \code{"original"}, \code{"tablets_freq"},
\code{"supply_based"}, \code{"actual_duration"}, or \code{"missing"}.}
}
}
\description{
Applies a four-step cascading imputation strategy to estimate the daily
dose in mg for each drug-exposure record. The cascade order and
tie-breaking rules match the \code{Baseline.qmd} analysis used in the
Phase 1 paper.
}
\details{
The first argument accepts either a \strong{connector} (created by
\code{\link[=create_omop_connector]{create_omop_connector()}} or \code{\link[=create_df_connector]{create_df_connector()}}) or a plain
\strong{data frame} for backward compatibility.
\subsection{Cascade order}{\tabular{lll}{
   Method \tab Label \tab Logic \cr
   M1 \tab \code{"original"} \tab Use \code{daily_dose} or \code{daily_dose_mg} column if numeric and > 0. \cr
   M2 \tab \code{"tablets_freq"} \tab \code{tablets * freq_per_day * strength_mg}. Requires \code{sig} column to be parsed first, or pre-parsed \code{tablets} and \code{freq_per_day} columns. \cr
   M3 \tab \code{"actual_duration"} \tab \code{(quantity * strength_mg) / actual_duration_days}, where \verb{actual_duration_days = end_date − start_date + 1}. This is the Burkard (2024) standard formula for fixed-amount oral formulations. \cr
   M4 \tab \code{"supply_based"} \tab \code{(quantity * strength_mg) / days_supply}. Fallback when date range is unavailable. \cr
}

}
}
\examples{
df <- tibble::tibble(
  person_id                = 1L,
  drug_source_value        = "PREDNISONE 5 MG TABLET",
  amount_value             = 5,
  quantity                 = 28,
  days_supply              = 28,
  drug_exposure_start_date = as.Date("2023-01-01"),
  drug_exposure_end_date   = as.Date("2023-01-28"),
  daily_dose               = NA_real_
)
calc_daily_dose_baseline(df)

# Connector path (synthetic example via df_connector):
con <- create_df_connector(df)
calc_daily_dose_baseline(con)
}
