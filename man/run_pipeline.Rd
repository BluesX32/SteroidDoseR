% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Run the full SteroidDoseR pipeline in a single call}
\usage{
run_pipeline(
  connector_or_df,
  method = c("baseline", "nlp"),
  drug_concept_ids = NULL,
  person_ids = NULL,
  start_date = NULL,
  end_date = NULL,
  sig_source = "sig",
  gap_days = 30L,
  equiv_table = NULL,
  return_level = c("episode", "exposure")
)
}
\arguments{
\item{connector_or_df}{A \code{steroid_connector} or data frame. Passed to the
selected dose method.}

\item{method}{\code{character(1)}. Imputation method: \code{"baseline"} (default) or
\code{"nlp"}.}

\item{drug_concept_ids, person_ids, start_date, end_date, sig_source}{Passed to the dose function. Ignored when \code{connector_or_df} is a data
frame.}

\item{gap_days}{\code{integer(1)}. Gap threshold for \code{\link[=build_episodes]{build_episodes()}}. Only
used when \code{return_level = "episode"}. Default: \code{30L}.}

\item{equiv_table}{Optional custom equivalency table. Passed to
\code{\link[=convert_pred_equiv]{convert_pred_equiv()}}. Default \code{NULL} uses the built-in table.}

\item{return_level}{\code{character(1)}. \code{"exposure"} returns one row per
drug-exposure record with dose columns appended. \code{"episode"} (default)
additionally runs \code{\link[=build_episodes]{build_episodes()}} and returns one row per
patient–drug episode.}
}
\value{
\itemize{
\item When \code{return_level = "exposure"}: the dose data frame from the chosen
method, with \code{pred_equiv_mg} and \code{equiv_factor} columns appended.
\item When \code{return_level = "episode"}: episode summary from \code{\link[=build_episodes]{build_episodes()}}.
}
}
\description{
Convenience wrapper that chains data extraction, daily-dose imputation,
prednisone-equivalency conversion, and optional episode building into a
single function call. Each step can also be run independently using the
individual exported functions.
}
\examples{
extdata <- system.file("extdata", package = "SteroidDoseR")
drug_exp <- readr::read_csv(
  file.path(extdata, "synthetic_drug_exposure.csv"),
  show_col_types = FALSE
)
con <- create_df_connector(drug_exp)
episodes <- run_pipeline(con, method = "baseline")
episodes[, c("person_id", "drug_name_std", "episode_start",
             "episode_end", "median_daily_dose")]
}
