% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gold_standard.R
\name{parse_dmard_dose}
\alias{parse_dmard_dose}
\title{Parse a vector of free-text DMARD dose strings}
\usage{
parse_dmard_dose(x)
}
\arguments{
\item{x}{Character vector of free-text dose strings (e.g. \code{"2g q6 months"},
\code{"1000 mg PO BID"}, \code{"1g/kg/q2 wk"}).}
}
\value{
A tibble with one row per element of \code{x} and columns:
\describe{
\item{dose_amount}{Numeric dose amount as written (before unit conversion).}
\item{dose_unit}{\code{"mg"} or \code{"g"}; \code{NA} when no unit found.}
\item{dose_per_kg}{\code{TRUE} when \code{/kg} detected; patient weight required
to compute a daily mg equivalent.}
\item{dose_frequency}{Standardised label: \code{"daily"}, \code{"bid"}, \code{"weekly"},
\code{"q2weeks"}, \code{"q4weeks"}, \code{"monthly"}, \code{"q3months"}, \code{"q6months"},
\code{"infusion_course"}, etc.; \code{NA} when not parseable.}
\item{freq_per_day}{Numeric doses per calendar day corresponding to
\code{dose_frequency} (e.g. \code{1/180} for \code{"q6months"}); \code{NA} when unknown.}
\item{dose_mg_per_admin}{\code{dose_amount} converted to mg (g * 1000);
\code{NA} when \code{dose_per_kg} is \code{TRUE}.}
\item{dose_daily_mg_equiv}{\code{dose_mg_per_admin * freq_per_day}; \code{NA}
when \code{dose_per_kg} is \code{TRUE} or frequency is unknown.}
\item{parse_status}{\code{"ok"}, \code{"weight_required"}, \code{"no_freq"},
\code{"no_unit"}, \code{"no_parse"}, \code{"empty"}, or \code{"error"}.}
}
}
\description{
Vectorised wrapper over the internal single-record parser. Each string is
normalised, then amount, unit, per-kg flag, and frequency are extracted via
regex. A daily mg equivalent is computed where possible. The function
\strong{never throws an error} -- malformed or empty inputs return a row of
\code{NA}s with an informative \code{parse_status}.
}
\examples{
parse_dmard_dose(c(
  "1000 mg PO BID",
  "2g q6 months",
  "1g/kg/q2 wk",
  "3g/d",
  "8.75",
  "every 6 months",
  "120 grams over 6 days"
))
}
