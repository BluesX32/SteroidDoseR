% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/episodes.R
\name{carry_forward_dose}
\alias{carry_forward_dose}
\title{Approximate trajectory truth by carrying the last reviewed gold dose forward}
\usage{
carry_forward_dose(
  visits_df,
  person_col = "person_id",
  visit_date_col = "visit_date",
  dose_col = "dose_mg",
  coverage_col = "has_coverage"
)
}
\arguments{
\item{visits_df}{Output of \code{\link[=dose_at_visits]{dose_at_visits()}} (or any data frame with the
same shape): one row per visit, with \code{person_col}, \code{visit_date_col},
\code{dose_col} (\code{NA} where unreviewed), and \code{coverage_col} (\code{TRUE} where
\code{dose_col} reflects an actual chart review).}

\item{person_col}{\code{character(1)}. Patient identifier column. Default
\code{"person_id"}.}

\item{visit_date_col}{\code{character(1)}. Encounter date column. Default
\code{"visit_date"}.}

\item{dose_col}{\code{character(1)}. Dose column to fill. Default \code{"dose_mg"}.}

\item{coverage_col}{\code{character(1)}. Logical column marking actually
reviewed visits. Default \code{"has_coverage"}.}
}
\value{
\code{visits_df}, row order preserved, with \code{dose_col} forward-filled
per patient and one column added, \code{dose_source}:
\describe{
\item{\code{"reviewed"}}{\code{coverage_col} was \code{TRUE} -- untouched, real review.}
\item{\code{"carried_forward"}}{\code{dose_col} is now non-\code{NA} only because of
the forward-fill.}
\item{\code{"unknown"}}{Still \code{NA} -- no reviewed visit yet exists for this
patient at or before this date.}
}
}
\description{
Implements the "same dose from one visit to the next" assumption: for each
patient, sorts visits chronologically and fills unreviewed visits with the
most recent \emph{actually reviewed} dose (last-observation-carried-forward,
never backward). This turns the sparse, visit-level gold coverage from
\code{\link[=dose_at_visits]{dose_at_visits()}} into a denser series usable for validating dose
\emph{trajectories} -- something the manually reviewed gold standard alone
cannot support, since it only covers discrete chart-reviewed intervals.
}
\details{
This is an assumption, not a fact: a patient's dose could genuinely change
between visits without being caught by chart review, and this function has
no way to detect that -- it will systematically understate real dose
variability (most acutely during tapers, when the dose is \emph{expected} to
change between visits). The output's \code{dose_source} column exists so
callers can always report accuracy separately for \code{"reviewed"} visits
(real chart review) versus \code{"reviewed"} + \code{"carried_forward"} visits
(includes the assumption) -- never blend the two into one number without
saying so.

Intended for gold-standard series specifically: run this on the output of
\code{dose_at_visits(gold_std, visits_df, ..., no_coverage_value = NA_real_)}.
It should not be run on a computed method's series, where \code{dose_mg = 0}
at an uncovered visit is already a real answer ("not on steroids"), not a
gap to fill.
}
\examples{
gold_at_visits <- tibble::tibble(
  person_id    = c(1L, 1L, 1L),
  visit_date   = as.Date(c("2023-01-01", "2023-02-01", "2023-03-01")),
  dose_mg      = c(10, NA, NA),
  has_coverage = c(TRUE, FALSE, FALSE)
)
carry_forward_dose(gold_at_visits)
}
\seealso{
\code{\link[=dose_at_visits]{dose_at_visits()}}, \code{\link[=dose_agreement_metrics]{dose_agreement_metrics()}}
}
