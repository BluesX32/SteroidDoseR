% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical.R
\name{tune_hierarchical_thresholds}
\alias{tune_hierarchical_thresholds}
\title{Grid-search match_tol and diff_threshold against a gold standard}
\usage{
tune_hierarchical_thresholds(
  drug_df,
  gold_df,
  match_tol_grid = c(0.5, 1, 2, 5),
  diff_thr_grid = c(3, 5, 10, 20),
  actions = c("nlp", "baseline"),
  gold_dose_col = "dose_daily_mg_equiv",
  ...
)
}
\arguments{
\item{drug_df}{Data frame of drug-exposure records (same input as
\code{\link{calc_daily_dose_hierarchical}()}).}

\item{gold_df}{Data frame of manually reviewed gold-standard episodes.
Must contain \code{person_id}, \code{episode_start}, \code{episode_end}, and a dose
column (see \code{gold_dose_col}).}

\item{match_tol_grid}{\code{numeric} vector. Values of \code{match_tol} to test.
Default: \code{c(0.5, 1, 2, 5)}.}

\item{diff_thr_grid}{\code{numeric} vector. Values of \code{diff_threshold} to test.
Default: \code{c(3, 5, 10, 20)}.}

\item{actions}{\code{character} vector. Which \code{moderate_disagreement_action}
values to test. Default: \code{c("nlp", "baseline")}.}

\item{gold_dose_col}{\code{character(1)}. Dose column in \code{gold_df}.
Default: \code{"dose_daily_mg_equiv"}.}

\item{...}{Additional arguments forwarded to \code{\link{calc_daily_dose_hierarchical}()}
for the initial parse (e.g. \code{sig_col}, \code{filter_oral}, \code{equiv_table}).}
}
\value{
A tibble with one row per parameter combination and columns:
\code{match_tol}, \code{diff_threshold}, \code{moderate_disagreement_action},
\code{n_matched}, \code{MAE}, \code{MBE}, \code{RMSE}, \code{coverage_pct}.
}
\description{
Runs \code{\link{calc_daily_dose_hierarchical}()} once to obtain the intermediate
\code{bl_dose} and \code{sig_dose} columns, then evaluates every combination of
\code{match_tol} x \code{diff_threshold} x \code{moderate_disagreement_action} using
\code{apply_adaptive_decision()} --- without re-running the baseline and NLP
parsers. Use the returned MAE grid to select parameters for the primary
analysis.
}
\examples{
\dontrun{
grid <- tune_hierarchical_thresholds(drug_df, gold_df)
grid[which.min(grid$MAE), ]
}
}
