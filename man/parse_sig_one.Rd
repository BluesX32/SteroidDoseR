% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlp.R
\name{parse_sig_one}
\alias{parse_sig_one}
\title{Parse one SIG (prescription instruction) string into dose components}
\usage{
parse_sig_one(sig_text)
}
\arguments{
\item{sig_text}{\code{character(1)}. The raw SIG string to parse.}
}
\value{
A one-row \code{tibble} with columns:
\describe{
\item{sig_raw}{Original input string.}
\item{tablets}{Number of tablets per dose (numeric).}
\item{freq_per_day}{Doses per day as a decimal (e.g. 0.5, 1, 2, 3, 4).}
\item{mg_per_admin}{Milligrams per single administration.}
\item{mg_total_flag}{\code{TRUE} if an explicit daily total was given.}
\item{duration_days}{Prescribed duration converted to days.}
\item{taper_flag}{\code{TRUE} if taper language detected.}
\item{prn_flag}{\code{TRUE} if "as needed" / PRN language detected.}
\item{free_text_flag}{\code{TRUE} if SIG is unstructured ("as directed", etc.)}
\item{daily_dose_mg}{Computed daily dose in mg.}
\item{parsed_status}{One of \code{"ok"}, \code{"taper"}, \code{"prn"}, \code{"free_text"},
\code{"no_parse"}, or \code{"empty"}.}
}
}
\description{
Applies a hierarchy of regex rules to extract tablets per dose, frequency
per day, mg amounts, duration, and flags. The function \strong{never throws an
error} -- malformed or empty inputs return a row of \code{NA}s with
\code{parsed_status = "empty"} or \code{"no_parse"}.
}
\examples{
parse_sig_one("Take 4 tablets (20 mg per dose) by mouth daily.")
parse_sig_one("Take 2 tabs BID for 14 days")
parse_sig_one("Taper by 1 mg every 4 weeks")
parse_sig_one("as directed")
parse_sig_one(NA_character_)
}
