% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval.R
\name{stratify_errors_by_parse}
\alias{stratify_errors_by_parse}
\title{Decompose evaluation errors by parse or method category}
\usage{
stratify_errors_by_parse(comparison_df, parse_col)
}
\arguments{
\item{comparison_df}{Data frame. Must contain the columns produced by
\code{\link[=evaluate_against_gold]{evaluate_against_gold()}}\verb{$comparison}: \code{computed_dose},
\code{gold_dose}, \code{absolute_error}, \code{bias_error},
\code{absolute_relative_error_pct}. Must also contain the column named by
\code{parse_col}. Join parse status from the episode data frame before
calling (see Examples).}

\item{parse_col}{\code{character(1)}. Name of the categorical parse/method column
to group by. Typical values: \code{"parsed_status"} (NLP advanced output),
\code{"sig_status"} (hierarchical output), \code{"hierarchical_method"}.}
}
\value{
A tibble with one row per category, sorted by descending \code{n}:
\describe{
\item{\code{category}}{The parse or method label.}
\item{\code{n}}{Total episodes in this category (matched or not).}
\item{\code{n_matched}}{Episodes with a non-\code{NA} computed dose.}
\item{\code{coverage_pct}}{\code{100 * n_matched / n}.}
\item{\code{MAE}}{Mean absolute error (mg prednisone-equivalent/day).}
\item{\code{MBE}}{Mean bias error (positive = over-estimation).}
\item{\code{MAPE}}{Mean absolute percentage error.}
\item{\code{median_AE}}{Median absolute error.}
}
}
\description{
Groups a comparison data frame (from
\code{\link[=evaluate_against_gold]{evaluate_against_gold()}}\verb{$comparison}) by a categorical column —
typically \code{"parsed_status"}, \code{"sig_status"}, or
\code{"hierarchical_method"} — and returns per-category error metrics.
This reveals whether errors concentrate in clinically interpretable
categories (e.g., taper, PRN, free-text), which reviewers need to assess
the validity of a dose-extraction paper.
}
\examples{
\dontrun{
# Build NLP episodes, propagating parsed_status for stratified eval
nlp_episodes <- build_episodes(
  nlp_df,
  end_col    = "drug_exposure_end_date",
  dose_col   = "daily_dose_mg",
  extra_cols = "parsed_status"
)
ev <- evaluate_against_gold(nlp_episodes, gold_df,
                             gold_dose_col = "dose_daily_mg_equiv")

# Join parsed_status onto comparison table
comp_with_parse <- ev$comparison |>
  dplyr::left_join(
    nlp_episodes |>
      dplyr::select(person_id, episode_start, parsed_status),
    by = c("person_id", "episode_start")
  )
stratify_errors_by_parse(comp_with_parse, "parsed_status")
}
}
\seealso{
\code{\link[=evaluate_against_gold]{evaluate_against_gold()}} — source of \code{comparison_df}.
\code{\link[=build_episodes]{build_episodes()}} \code{extra_cols} argument — how to carry parse status
into episode-level output.
}
