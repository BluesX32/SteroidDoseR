% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval.R
\name{evaluate_detection}
\alias{evaluate_detection}
\title{Evaluate binary steroid detection against two gold standards}
\usage{
evaluate_detection(
  computed_df,
  gold_positive_df,
  gold_negative_df,
  detection_threshold = 0,
  obs_window_source = c("computed", "explicit", "study"),
  study_start = NULL,
  study_end = NULL,
  computed_id_col = "person_id",
  computed_start_col = "episode_start",
  computed_end_col = "episode_end",
  computed_dose_col = "median_daily_dose",
  gold_pos_id_col = "patient_id",
  gold_pos_start_col = "episode_start",
  gold_pos_end_col = "episode_end",
  gold_neg_id_col = "patient_id",
  gold_neg_start_col = "obs_start",
  gold_neg_end_col = "obs_end"
)
}
\arguments{
\item{computed_df}{Data frame of computed episode summaries (output of
\code{\link[=build_episodes]{build_episodes()}} after \code{\link[=convert_pred_equiv]{convert_pred_equiv()}}).}

\item{gold_positive_df}{Data frame of confirmed steroid-user episodes.
Must contain \code{gold_pos_id_col}, \code{gold_pos_start_col}, \code{gold_pos_end_col}.}

\item{gold_negative_df}{Data frame of confirmed steroid-naive patients
(one row per patient). Must contain \code{gold_neg_id_col}. When
\code{obs_window_source = "explicit"} also requires \code{gold_neg_start_col} and
\code{gold_neg_end_col}.}

\item{detection_threshold}{\code{numeric(1)}. Minimum computed dose (mg
pred-equiv/day) to count as detected. Default \code{0} (any positive dose).
Use \code{5} or \code{10} to exclude trace / data-artifact doses.}

\item{obs_window_source}{\code{character(1)}. How to define the observation
window for gold-negative patients. One of \code{"computed"} (default),
\code{"explicit"}, or \code{"study"}. See Details.}

\item{study_start}{Date or \code{character}. Study start date. Required when
\code{obs_window_source = "study"}.}

\item{study_end}{Date or \code{character}. Study end date. Required when
\code{obs_window_source = "study"}.}

\item{computed_id_col}{\code{character(1)}. Patient ID column in \code{computed_df}.
Default \code{"person_id"}.}

\item{computed_start_col}{\code{character(1)}. Episode start column in
\code{computed_df}. Default \code{"episode_start"}.}

\item{computed_end_col}{\code{character(1)}. Episode end column in \code{computed_df}.
Default \code{"episode_end"}.}

\item{computed_dose_col}{\code{character(1)}. Dose column in \code{computed_df}.
Default \code{"median_daily_dose"}.}

\item{gold_pos_id_col}{\code{character(1)}. Patient ID column in
\code{gold_positive_df}. Default \code{"patient_id"}.}

\item{gold_pos_start_col}{\code{character(1)}. Episode start column in
\code{gold_positive_df}. Default \code{"episode_start"}.}

\item{gold_pos_end_col}{\code{character(1)}. Episode end column in
\code{gold_positive_df}. Default \code{"episode_end"}.}

\item{gold_neg_id_col}{\code{character(1)}. Patient ID column in
\code{gold_negative_df}. Default \code{"patient_id"}.}

\item{gold_neg_start_col}{\code{character(1)}. Observation start column in
\code{gold_negative_df}. Used only when \code{obs_window_source = "explicit"}.
Default \code{"obs_start"}.}

\item{gold_neg_end_col}{\code{character(1)}. Observation end column in
\code{gold_negative_df}. Used only when \code{obs_window_source = "explicit"}.
Default \code{"obs_end"}.}
}
\value{
A named list with four elements:
\describe{
  \item{\code{$confusion}}{2x2 matrix. Rows = Gold (Positive / Negative);
    columns = Computed (Detected / Not detected). Cells: TP, FN, FP, TN.}
  \item{\code{$metrics}}{One-row tibble: \code{n_gold_positive}, \code{n_gold_negative},
    \code{TP}, \code{FN}, \code{FP}, \code{TN}, \code{sensitivity}, \code{specificity},
    \code{PPV}, \code{NPV}, \code{accuracy}, \code{F1}, \code{kappa}.}
  \item{\code{$detail_positive}}{Gold-positive rows with added \code{detected}
    (logical) and \code{classification} (\code{"TP"} / \code{"FN"}).}
  \item{\code{$detail_negative}}{Gold-negative rows with added \code{detected}
    (logical) and \code{classification} (\code{"FP"} / \code{"TN"}).}
}
}
\description{
Computes TP / FP / TN / FN and Cohen's kappa from two gold-standard
cohorts: patients confirmed to be on steroids (\code{gold_positive_df}) and
patients confirmed to have never used steroids (\code{gold_negative_df}).
}
\details{
\strong{Gold positives — episode-level (TP / FN):}
A gold-positive episode is \strong{True Positive} when a computed episode for
the same patient overlaps it and has \code{computed_dose >= detection_threshold}.
Otherwise it is \strong{False Negative}.

\strong{Gold negatives — patient-level (FP / TN):}
A gold-negative patient is \strong{False Positive} when the algorithm assigns
them a dose \code{>= detection_threshold} within their observation window.
Otherwise it is \strong{True Negative}. The window is set by \code{obs_window_source}:
\describe{
  \item{\code{"computed"} (default)}{Window = all computed episodes for that
    patient. Patients absent from \code{computed_df} are automatically TN.
    Preferred when no explicit observation window is available.}
  \item{\code{"explicit"}}{Each row of \code{gold_negative_df} carries its own
    window via \code{gold_neg_start_col} / \code{gold_neg_end_col} (e.g. the
    patient's enrolment period).}
  \item{\code{"study"}}{A single fixed window (\code{study_start} to \code{study_end})
    applies to every gold-negative patient.}
}
}
\examples{
computed <- tibble::tibble(
  person_id         = c(1L, 2L),
  episode_start     = as.Date(c("2023-01-01", "2023-03-01")),
  episode_end       = as.Date(c("2023-06-30", "2023-09-30")),
  median_daily_dose = c(20, 5)
)
gold_pos <- tibble::tibble(
  patient_id    = 1L,
  episode_start = as.Date("2023-01-01"),
  episode_end   = as.Date("2023-06-30")
)
gold_neg <- tibble::tibble(patient_id = c(3L, 4L))
evaluate_detection(computed, gold_pos, gold_neg)
}
