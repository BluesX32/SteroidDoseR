% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlp_notes.R
\name{calc_daily_dose_nlp_notes}
\alias{calc_daily_dose_nlp_notes}
\title{Compute daily steroid doses from clinical note text (medspaCy method)}
\usage{
calc_daily_dose_nlp_notes(
  connector_or_df,
  note_col = "clinical_note",
  sig_col = "sig",
  drug_name_col = "drug_concept_name",
  filter_oral = TRUE,
  max_daily_dose_mg = 2000,
  baseline_fallback = FALSE,
  equiv_table = NULL,
  drug_name_map = NULL,
  drug_concept_ids = NULL,
  person_ids = NULL,
  start_date = NULL,
  end_date = NULL,
  sig_source = "sig"
)
}
\arguments{
\item{connector_or_df}{A \code{steroid_connector} or a data frame.}

\item{note_col}{\code{character(1)}. Column containing clinical note text.
Default: \code{"clinical_note"}. Rows with \code{NA} in this column skip the note
parsing step.}

\item{sig_col}{\code{character(1)}. Column with the SIG text. Default: \code{"sig"}.}

\item{drug_name_col}{\code{character(1)}. Column with the drug name.
Default: \code{"drug_concept_name"}.}

\item{filter_oral}{\code{logical(1)}. If \code{TRUE} (default), only oral-route
records are kept.}

\item{max_daily_dose_mg}{\code{numeric(1)} or \code{NULL}. Upper plausibility bound.
Default: \code{2000}. Set to \code{NULL} to disable.}

\item{baseline_fallback}{\code{logical(1)}. If \code{TRUE}, carries through a
pre-existing \code{daily_dose_mg_orig} column for failed records. Default \code{FALSE}.}

\item{equiv_table}{Optional prednisone-equivalency table (columns
\code{drug_name_std}, \code{pred_equiv_factor}). Default: built-in table.}

\item{drug_name_map}{Optional data frame for site-specific drug name
overrides, passed to \code{\link[=standardize_drug_name]{standardize_drug_name()}}. Default: \code{NULL}.}

\item{drug_concept_ids, person_ids, start_date, end_date, sig_source}{Connector-path filtering arguments. Ignored for plain data frames.}
}
\value{
A data frame with the same columns as
\code{\link[=calc_daily_dose_nlp_advanced]{calc_daily_dose_nlp_advanced()}} plus note-specific columns added by
\code{\link[=parse_note_one]{parse_note_one()}}: \code{note_entity_count}, \code{note_negation_flag},
\code{note_uncertainty_flag}, \code{note_section}.
}
\description{
Extends \code{\link[=calc_daily_dose_nlp_advanced]{calc_daily_dose_nlp_advanced()}} with a clinical note parsing step.
The pipeline runs in this order:
\enumerate{
\item \strong{Regex SIG parser} (\code{\link[=parse_sig_one_advanced]{parse_sig_one_advanced()}}) on the \code{sig} column —
fast path for structured prescription instructions.
\item \strong{medspaCy} on \code{note_col} — for rows the regex could not resolve
(\code{taper}, \code{free_text}, \code{no_parse}, \code{empty} statuses).
\item \strong{Strength fallback} — \code{amount_value} (unit-checked) → drug concept name.
\item \strong{Baseline structural fallback} (M1/M3/M4) — for rows still \code{NA}.
\item \strong{Dose plausibility cap} (\code{max_daily_dose_mg}).
}

Requires Python + medspaCy installed (see \code{\link[=parse_note_one]{parse_note_one()}} for setup).
}
\examples{
\dontrun{
df <- tibble::tibble(
  person_id                = 1L,
  drug_concept_name        = "prednisone 5 MG oral tablet",
  route_concept_name       = "oral",
  sig                      = NA_character_,
  clinical_note            = paste(
    "Assessment: Patient continued on prednisone 40 mg daily.",
    "Plan: Taper to 20 mg over 4 weeks."
  ),
  drug_exposure_start_date = as.Date("2023-01-01"),
  drug_exposure_end_date   = as.Date("2023-06-01")
)
calc_daily_dose_nlp_notes(df, note_col = "clinical_note")
}
}
\seealso{
\code{\link[=parse_note_one]{parse_note_one()}}, \code{\link[=parse_notes]{parse_notes()}},
\code{\link[=calc_daily_dose_nlp_advanced]{calc_daily_dose_nlp_advanced()}}
}
