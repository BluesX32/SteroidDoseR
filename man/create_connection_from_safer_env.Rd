% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{create_connection_from_safer_env}
\alias{create_connection_from_safer_env}
\title{Create a SAFER/REACH Databricks connection from an env file}
\usage{
create_connection_from_safer_env(env_file = "R.env")
}
\arguments{
\item{env_file}{Path to the credentials file. Default \code{"R.env"} (the SAFER
convention); falls back to \code{".env"} if \code{"R.env"} does not exist.}
}
\value{
An \code{omop_connector} object with \code{$use_rjdbc = TRUE}.
}
\description{
Loads credentials from a \code{.env} or \code{R.env} file and calls
\code{\link[=create_safer_connection]{create_safer_connection()}}. This is the recommended entry point for
scripts running on the Johns Hopkins SAFER / REACH HPC cluster.
}
\examples{
\dontrun{
con <- create_connection_from_safer_env("R.env")
episodes <- run_pipeline(con, method = "baseline")
disconnect_connector(con)
}
}
