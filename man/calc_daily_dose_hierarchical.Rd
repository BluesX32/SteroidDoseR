% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical.R
\name{calc_daily_dose_hierarchical}
\alias{calc_daily_dose_hierarchical}
\title{Compute daily steroid doses using the hierarchical Baseline + NLP method}
\usage{
calc_daily_dose_hierarchical(
  connector_or_df,
  sig_col = "sig",
  drug_name_col = "drug_concept_name",
  filter_oral = TRUE,
  diff_threshold = 5,
  match_tol = 0.01,
  expand_tapers = FALSE,
  max_daily_dose_mg = 2000,
  equiv_table = NULL,
  drug_name_map = NULL,
  drug_concept_ids = NULL,
  person_ids = NULL,
  start_date = NULL,
  end_date = NULL,
  sig_source = "sig"
)
}
\arguments{
\item{connector_or_df}{A \code{steroid_connector} or a data frame.}

\item{sig_col}{\code{character(1)}. Column with SIG text. Default: \code{"sig"}.}

\item{drug_name_col}{\code{character(1)}. Column with drug name.
Default: \code{"drug_concept_name"}.}

\item{filter_oral}{\code{logical(1)}. Keep only oral-route records.
Default: \code{TRUE}.}

\item{diff_threshold}{\code{numeric(1)}. Relative-difference threshold (ratio).
When \code{|bl - nlp| / max(bl, nlp)} exceeds this value the NLP result
overrides baseline. Default: \code{5}.}

\item{match_tol}{\code{numeric(1)}. Relative-difference threshold at or below
which the two estimates are considered to match. Records below this are
labelled \code{"cross_checked"}. Default: \code{0.01}.}

\item{expand_tapers}{\code{logical(1)}. When \code{TRUE}, taper SIGs handled by the
NLP branch are expanded into per-step rows via
\code{\link[=parse_taper_schedule]{parse_taper_schedule()}}. Default: \code{FALSE}.}

\item{max_daily_dose_mg}{\code{numeric(1)} or \code{NULL}. Plausibility cap. Records
above this value are set to \code{NA} with a warning. Default: \code{2000}.}

\item{equiv_table, drug_name_map, drug_concept_ids, person_ids, start_date, end_date, sig_source}{Passed to the underlying imputation functions.}
}
\value{
A data frame with the original columns plus:
\describe{
\item{\code{drug_name_std}}{Standardised drug name.}
\item{\code{bl_dose}}{Baseline daily dose (mg/day) from M1/M3/M4.}
\item{\code{bl_method}}{Baseline imputation method used.}
\item{\code{sig_dose}}{NLP daily dose (mg/day) from SIG text + strength fallback.}
\item{\code{sig_status}}{NLP parsed status.}
\item{\code{sig_tablets}}{Tablets per dose from SIG.}
\item{\code{sig_freq_per_day}}{Doses per day from SIG.}
\item{\code{sig_mg_per_admin}}{mg per administration from SIG.}
\item{\code{sig_taper_flag}}{\code{TRUE} if taper language detected in SIG.}
\item{\code{sig_duration_days}}{Prescribed duration from SIG.}
\item{\code{daily_dose_mg}}{Final hierarchical daily dose (mg/day).}
\item{\code{hierarchical_method}}{One of \code{"cross_checked"}, \code{"nlp_override"},
\code{"blended"}, \code{"baseline_only"}, \code{"nlp_fills_baseline"}, \code{"nlp_taper"},
\code{"missing"}, \code{"implausible"}.}
}
}
\description{
Runs Baseline (M1/M3/M4) and NLP Advanced (SIG parser) independently, then
applies a per-row decision tree that cross-checks the two estimates and
selects the most appropriate result.
}
\details{
\strong{Decision logic}

\tabular{lll}{
   Baseline \tab SIG \tab Outcome \cr
   complete \tab complete \tab Cross-check; blend or override based on \code{diff_threshold} \cr
   complete \tab absent / unparseable \tab Baseline only \cr
   incomplete \tab complete, steady \tab NLP fills missing baseline fields \cr
   incomplete \tab complete, taper \tab NLP calculates dose from taper SIG \cr
   incomplete \tab absent / unparseable \tab \code{NA} \cr
}

\strong{Cross-check resolution:}
\itemize{
\item \code{|diff| <= match_tol} → \code{"cross_checked"} (use NLP value; both agree)
\item \code{match_tol < |diff| <= diff_threshold} → \code{"blended"} (average of both)
\item \code{|diff| > diff_threshold} → \code{"nlp_override"} (use NLP; large discrepancy)
}

\strong{Independence of the two sides:}
Baseline uses M1 (original daily_dose column), M3 (quantity / actual duration),
and M4 (quantity / days_supply) only. M2 (tablets x freq x strength) is
excluded so the baseline and NLP estimates are derived from independent data
sources, making the cross-check meaningful.
}
\examples{
df <- tibble::tibble(
  person_id                = 1:3,
  drug_concept_name        = rep("prednisone 5 MG oral tablet", 3),
  route_concept_name       = rep("oral", 3),
  sig = c(
    "Take 2 tablets (10 mg) daily",
    "Take 1 tablet daily",
    NA_character_
  ),
  quantity                 = c(60, 90, 90),
  days_supply              = c(30, 90, 90),
  amount_value             = c(5, 5, 5),
  amount_unit_concept_id   = c(8576L, 8576L, 8576L),
  drug_exposure_start_date = rep(as.Date("2023-01-01"), 3),
  drug_exposure_end_date   = rep(as.Date("2023-02-01"), 3)
)
calc_daily_dose_hierarchical(df)
}
\seealso{
\code{\link[=calc_daily_dose_baseline]{calc_daily_dose_baseline()}}, \code{\link[=calc_daily_dose_nlp_advanced]{calc_daily_dose_nlp_advanced()}}
}
