% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{create_omop_connection}
\alias{create_omop_connection}
\title{Create a database connection with platform-specific optimisations}
\usage{
create_omop_connection(
  dbms = NULL,
  server = NULL,
  database = NULL,
  port = NULL,
  user = NULL,
  password = NULL,
  use_windows_auth = FALSE,
  connectionString = NULL,
  pathToDriver = NULL,
  cdm_schema = NULL,
  vocabulary_schema = NULL,
  results_schema = NULL,
  extraSettings = NULL,
  use_env = TRUE
)
}
\arguments{
\item{dbms}{Character: \code{"sql server"}, \code{"spark"}, \code{"databricks"},
\code{"postgresql"}, \code{"redshift"}, etc.}

\item{server}{Server name or address.}

\item{database}{Database name.}

\item{port}{Port number (optional; defaults are applied per platform).}

\item{user}{Username (optional for Windows auth).}

\item{password}{Password (optional for Windows auth).}

\item{use_windows_auth}{Logical. Use Windows AD authentication (SQL Server
only). Default \code{FALSE}.}

\item{connectionString}{Full JDBC connection string (optional; skips
auto-build).}

\item{pathToDriver}{Path to the JDBC driver folder.}

\item{cdm_schema}{CDM schema name.}

\item{vocabulary_schema}{Vocabulary schema (defaults to \code{cdm_schema}).}

\item{results_schema}{Results schema (optional).}

\item{extraSettings}{Additional JDBC settings string (e.g., Databricks
\code{httpPath}).}

\item{use_env}{Logical. Load unset parameters from environment variables.
Default \code{TRUE}.}
}
\value{
An \code{omop_connector} object (\code{c("omop_connector", "steroid_connector")}).
The database connection is lazy -- opened per-query and closed immediately
after. No manual \code{disconnect()} call is needed. Pass directly to
\code{calc_daily_dose_baseline()}, \code{run_pipeline()}, etc.
}
\description{
Supports multiple database platforms with appropriate configurations:
\itemize{
\item SQL Server: Standard auth and Windows AD (Integrated Security)
\item Databricks/Spark: JDBC with Arrow optimisation
\item PostgreSQL/Redshift: Standard JDBC connections
\item BigQuery: For All of Us environment
}
}
\details{
All parameters can be omitted when \code{use_env = TRUE} (the default); the
function then reads them from the environment variables listed below.
\subsection{Environment variables}{\tabular{ll}{
   Variable \tab Description \cr
   \code{SQL_DBMS} / \code{DB_TYPE} / \code{OMOP_ENV} \tab DBMS type (default: \code{"sql server"}) \cr
   \code{SQL_SERVER} / \code{DB_SERVER} \tab Server address \cr
   \code{SQL_DATABASE} / \code{DB_DATABASE} \tab Database name \cr
   \code{SQL_PORT} / \code{DB_PORT} \tab Port number \cr
   \code{SQL_USER} / \code{DB_USER} \tab Username \cr
   \code{SQL_PASSWORD} / \code{DB_PASSWORD} \tab Password \cr
   \code{SQL_JDBC_PATH} / \code{JDBC_DRIVER_PATH} \tab Path to JDBC driver folder \cr
   \code{SQL_CDM_SCHEMA} / \code{CDM_SCHEMA} \tab CDM schema (default: \code{"dbo"}) \cr
   \code{SQL_VOCABULARY_SCHEMA} / \code{VOCABULARY_SCHEMA} \tab Vocabulary schema \cr
   \code{SQL_RESULTS_SCHEMA} / \code{RESULTS_SCHEMA} \tab Results schema \cr
   \code{USE_WINDOWS_AUTH} \tab \code{"true"} / \code{"1"} / \code{"yes"} to use Windows AD auth \cr
   \code{SQL_CDM_DATABASE} \tab Prepended to CDM schema as \code{database.schema} \cr
   \code{DB_EXTRA_SETTINGS} \tab Extra JDBC settings string (Databricks \code{HTTPPath}) \cr
   \code{ENABLE_ARROW} \tab \code{"TRUE"} to enable Databricks Arrow optimisation \cr
}

}
}
\examples{
\dontrun{
# SQL Server with Windows AD authentication
con <- create_omop_connection(
  dbms             = "sql server",
  server           = "server.domain.com",
  database         = "OMOP_CDM",
  use_windows_auth = TRUE,
  cdm_schema       = "dbo"
)

# SQL Server with standard authentication
con <- create_omop_connection(
  dbms       = "sql server",
  server     = "server.domain.com",
  database   = "OMOP_CDM",
  user       = "username",
  password   = "password",
  cdm_schema = "dbo"
)

# Databricks with token authentication
con <- create_omop_connection(
  dbms          = "databricks",
  server        = "workspace.cloud.databricks.com",
  database      = "default",
  user          = "token",
  password      = Sys.getenv("DATABRICKS_TOKEN"),
  cdm_schema    = "omop.data",
  extraSettings = "httpPath=/sql/1.0/warehouses/warehouse_id"
)

# Simplest: load everything from environment variables / .env file
con <- create_connection_from_env(".env")

# Use the connection with SteroidDoseR pipeline functions
episodes <- run_pipeline(con, method = "baseline")
DatabaseConnector::disconnect(con)
}
}
