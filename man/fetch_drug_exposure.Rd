% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connector.R
\name{fetch_drug_exposure}
\alias{fetch_drug_exposure}
\title{Fetch drug exposure records from a connector (internal)}
\usage{
fetch_drug_exposure(
  connector,
  drug_concept_ids = NULL,
  person_ids = NULL,
  start_date = NULL,
  end_date = NULL,
  sig_source = "sig"
)
}
\arguments{
\item{connector}{A \code{steroid_connector} (with \verb{$conn} set for omop type).}

\item{drug_concept_ids}{Integer vector of \code{drug_concept_id} values to
include, or \code{NULL} to include all.}

\item{person_ids}{Vector of \code{person_id} values to include, or \code{NULL}.}

\item{start_date}{Character/Date lower bound on \code{drug_exposure_start_date}.
Default \code{"1900-01-01"}.}

\item{end_date}{Character/Date upper bound on \code{drug_exposure_start_date}.
Default today.}

\item{sig_source}{\code{character(1)}. Which column to use as the SIG text.
\code{"sig"} (default) uses \code{drug_exposure.sig}. \code{"drug_source_value"} aliases
\code{drug_source_value} into \code{sig} when the native SIG column is absent or
all-NA.}
}
\value{
A tibble conforming to the \link{drug_df_contract}.
}
\description{
Dispatches on connector type. For \code{omop_connector}, renders and executes
\code{inst/sql/extract_drug_exposure.sql}. For \code{df_connector}, optionally
filters the stored data frame.
}
\details{
This function is always called inside \code{\link[=with_connector]{with_connector()}} so that
\code{omop_connector$conn} is guaranteed to be set.
}
